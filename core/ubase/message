[1mNOTE:[m

      Carbs Linux init by default uses busybox getty, rather than
      ubase getty (which has a different argument scheme). You can
      change the getty command from '[1m/etc/init/getty.boot[m'.
